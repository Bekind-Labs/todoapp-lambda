name: Run tests against pull requests

on: pull_request

jobs:

  test:
    runs-on: ubuntu-latest
    services:
      localstack:
        image: localstack/localstack
        ports:
          - "4566:4566"
        volumes:
          - /var/run/docker.sock:/var/run/docker.sock
        env:
          DISABLE_CORS_CHECKS: 1
          DYNAMODB_SHARE_DB: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Initialize Terraform
        working-directory: ./terraform
        env:
          AWS_ENDPOINT_URL: http://localhost:4566/
          AWS_REGION: ap-northeast-1
          AWS_ACCESS_KEY_ID: xxx
          AWS_SECRET_ACCESS_KEY: yyy
          STATE_BUCKET: local-terraform-state
        run: |
          set -x
          aws --endpoint=$AWS_ENDPOINT_URL s3 mb s3://$STATE_BUCKET
          terraform init -backend-config=bucket=$STATE_BUCKET

      - name: Initialize LocalStack resources
        working-directory: ./terraform
        env:
          AWS_ENDPOINT_URL: http://localhost:4566/
          AWS_REGION: ap-northeast-1
          AWS_ACCESS_KEY_ID: xxx
          AWS_SECRET_ACCESS_KEY: yyy
          TF_VAR_architecture: x86_64
        run: terraform apply -auto-approve

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Run Tests
        run: make ci test

      - name: Run E2E tests
        run: cd ./app && make ci e2etest
